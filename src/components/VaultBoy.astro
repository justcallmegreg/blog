---
// Decorative Vault Boy GIF in the bottom-right corner. The /vaultboy.gif
// endpoint serves a copy whose native loop count is set from config, so the
// browser plays it that many times and freezes on the last frame. Hidden under
// prefers-reduced-motion since it's purely decorative.
---
<img class="vaultboy" src="/vaultboy.gif" alt="" aria-hidden="true" />
